// @TASK P4-R1-T1 - 기타 용어 데이터
// @SPEC docs/planning/sqld-visual-lab-spec.md

import { Term } from '@/types'

export const otherTerms: Term[] = [
  {
    id: 'index',
    name: 'INDEX',
    category: 'OTHER',
    definition: '데이터 검색 속도를 향상시키기 위한 데이터 구조입니다. 자주 조회하는 컬럼에 인덱스를 생성하면 SELECT 성능이 크게 개선되지만, INSERT/UPDATE/DELETE 성능은 약간 저하될 수 있습니다.',
    syntax: 'CREATE [UNIQUE] INDEX 인덱스명\nON 테이블명 (컬럼명1, 컬럼명2);\n\nDROP INDEX 인덱스명;',
    exampleSQL: 'CREATE INDEX idx_students_name\nON students (name);\n\n-- 유일 인덱스\nCREATE UNIQUE INDEX idx_students_email\nON students (email);',
    exampleResult: 'name 컬럼에 인덱스가 생성되어 WHERE name = \'홍길동\' 같은 쿼리가 훨씬 빠르게 실행됩니다.',
    relatedTerms: ['primary-key', 'unique'],
  },
  {
    id: 'view',
    name: 'VIEW',
    category: 'OTHER',
    definition: '하나 이상의 테이블을 기반으로 만든 가상 테이블입니다. 복잡한 쿼리를 단순화하고, 특정 사용자에게 필요한 데이터만 보여주는 보안 목적으로도 활용됩니다.',
    syntax: 'CREATE [OR REPLACE] VIEW 뷰이름 AS\n  SELECT 컬럼명 FROM 테이블명 WHERE 조건;\n\nDROP VIEW 뷰이름;',
    exampleSQL: 'CREATE VIEW senior_students AS\n  SELECT id, name\n  FROM students\n  WHERE grade >= 3;\n\n-- 뷰 사용\nSELECT * FROM senior_students;',
    exampleResult: 'senior_students 뷰를 통해 grade가 3 이상인 학생만 조회됩니다. 실제 데이터는 students 테이블에 저장됩니다.',
    relatedTerms: ['select', 'index'],
  },
  {
    id: 'trigger',
    name: 'TRIGGER',
    category: 'OTHER',
    definition: '특정 이벤트(INSERT, UPDATE, DELETE)가 발생했을 때 자동으로 실행되는 프로시저입니다. 데이터 무결성 유지, 감사 로그 기록 등에 활용됩니다.',
    syntax: 'CREATE TRIGGER 트리거명\n  BEFORE|AFTER INSERT|UPDATE|DELETE\n  ON 테이블명\n  FOR EACH ROW\nBEGIN\n  -- 실행 내용\nEND;',
    exampleSQL: 'CREATE TRIGGER log_student_insert\n  AFTER INSERT ON students\n  FOR EACH ROW\nBEGIN\n  INSERT INTO audit_log (action, student_id, created_at)\n  VALUES (\'INSERT\', NEW.id, NOW());\nEND;',
    exampleResult: 'students 테이블에 새 행이 삽입될 때마다 자동으로 audit_log 테이블에 기록이 추가됩니다.',
    relatedTerms: ['constraint', 'view'],
  },
  {
    id: 'constraint',
    name: 'CONSTRAINT',
    category: 'OTHER',
    definition: '테이블에 저장되는 데이터의 무결성을 보장하기 위한 규칙입니다. PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK 등이 대표적인 제약조건입니다.',
    syntax: '-- 컬럼 수준\nCREATE TABLE 테이블명 (\n  컬럼명 데이터타입 CONSTRAINT 제약조건명 제약조건타입\n);\n\n-- 테이블 수준\nCONSTRAINT 제약조건명 제약조건타입 (컬럼명)',
    exampleSQL: 'CREATE TABLE orders (\n  order_id INTEGER CONSTRAINT pk_orders PRIMARY KEY,\n  amount INTEGER CONSTRAINT chk_amount CHECK (amount > 0),\n  student_id INTEGER,\n  CONSTRAINT fk_student FOREIGN KEY (student_id)\n    REFERENCES students(id)\n);',
    exampleResult: 'orders 테이블에 기본키, 체크 제약조건, 외래키가 적용됩니다. 잘못된 데이터 입력이 자동으로 차단됩니다.',
    relatedTerms: ['primary-key', 'foreign-key', 'unique', 'not-null', 'check'],
  },
  {
    id: 'primary-key',
    name: 'PRIMARY KEY',
    category: 'OTHER',
    definition: '테이블에서 각 행을 고유하게 식별하는 컬럼 또는 컬럼 조합입니다. NULL 값을 가질 수 없으며, 중복 값이 허용되지 않습니다. 테이블당 하나만 정의할 수 있습니다.',
    syntax: '-- 컬럼 수준\n컬럼명 데이터타입 PRIMARY KEY\n\n-- 테이블 수준 (복합키)\nPRIMARY KEY (컬럼명1, 컬럼명2)',
    exampleSQL: 'CREATE TABLE students (\n  id INTEGER PRIMARY KEY,\n  name VARCHAR(50) NOT NULL\n);\n\n-- 복합 기본키\nCREATE TABLE enrollments (\n  student_id INTEGER,\n  course_id INTEGER,\n  PRIMARY KEY (student_id, course_id)\n);',
    exampleResult: 'id 컬럼이 기본키로 설정됩니다. 같은 id 값을 가진 행은 삽입할 수 없고, NULL 값도 허용되지 않습니다.',
    relatedTerms: ['foreign-key', 'unique', 'not-null', 'constraint'],
  },
  {
    id: 'foreign-key',
    name: 'FOREIGN KEY',
    category: 'OTHER',
    definition: '다른 테이블의 기본키를 참조하는 컬럼입니다. 참조 무결성을 보장하여 존재하지 않는 부모 데이터를 참조하는 것을 방지합니다.',
    syntax: 'FOREIGN KEY (컬럼명)\n  REFERENCES 참조테이블(참조컬럼)\n  [ON DELETE CASCADE|SET NULL|RESTRICT]\n  [ON UPDATE CASCADE|SET NULL|RESTRICT]',
    exampleSQL: 'CREATE TABLE orders (\n  id INTEGER PRIMARY KEY,\n  student_id INTEGER,\n  FOREIGN KEY (student_id)\n    REFERENCES students(id)\n    ON DELETE CASCADE\n);',
    exampleResult: 'orders.student_id는 students.id를 참조합니다. students 테이블에 없는 id는 삽입할 수 없고, 학생이 삭제되면 해당 주문도 자동 삭제됩니다.',
    relatedTerms: ['primary-key', 'constraint'],
  },
  {
    id: 'unique',
    name: 'UNIQUE',
    category: 'OTHER',
    definition: '컬럼의 모든 값이 고유해야 함을 보장합니다. PRIMARY KEY와 달리 NULL 값은 허용되고(DBMS마다 다름), 테이블에 여러 개 정의할 수 있습니다.',
    syntax: '-- 컬럼 수준\n컬럼명 데이터타입 UNIQUE\n\n-- 테이블 수준\nCONSTRAINT 제약조건명 UNIQUE (컬럼명)',
    exampleSQL: 'CREATE TABLE students (\n  id INTEGER PRIMARY KEY,\n  email VARCHAR(100) UNIQUE,\n  name VARCHAR(50) NOT NULL\n);',
    exampleResult: 'email 컬럼에 같은 이메일 주소를 가진 학생은 두 명 이상 등록할 수 없습니다. PRIMARY KEY와 달리 NULL은 허용됩니다.',
    relatedTerms: ['primary-key', 'constraint', 'index'],
  },
  {
    id: 'not-null',
    name: 'NOT NULL',
    category: 'OTHER',
    definition: '해당 컬럼에 NULL 값이 저장되는 것을 금지합니다. 반드시 값이 있어야 하는 필수 항목에 사용합니다.',
    syntax: '컬럼명 데이터타입 NOT NULL',
    exampleSQL: 'CREATE TABLE students (\n  id INTEGER PRIMARY KEY,\n  name VARCHAR(50) NOT NULL,\n  email VARCHAR(100)\n);',
    exampleResult: 'name 컬럼은 NULL 값을 가질 수 없어 반드시 이름을 입력해야 합니다. email은 NULL이 허용됩니다.',
    relatedTerms: ['constraint', 'check', 'default'],
  },
  {
    id: 'check',
    name: 'CHECK',
    category: 'OTHER',
    definition: '컬럼에 저장될 수 있는 값의 범위나 조건을 지정합니다. 조건을 만족하지 않는 값은 삽입/수정이 거부됩니다.',
    syntax: '-- 컬럼 수준\n컬럼명 데이터타입 CHECK (조건식)\n\n-- 테이블 수준\nCONSTRAINT 제약조건명 CHECK (조건식)',
    exampleSQL: 'CREATE TABLE students (\n  id INTEGER PRIMARY KEY,\n  name VARCHAR(50) NOT NULL,\n  grade INTEGER CHECK (grade BETWEEN 1 AND 4),\n  age INTEGER CHECK (age >= 18)\n);',
    exampleResult: 'grade는 1~4 사이 값만 허용되고, age는 18 이상이어야 합니다. 조건에 맞지 않는 데이터는 오류가 발생합니다.',
    relatedTerms: ['constraint', 'not-null', 'default'],
  },
  {
    id: 'default',
    name: 'DEFAULT',
    category: 'OTHER',
    definition: '행을 삽입할 때 해당 컬럼의 값을 지정하지 않으면 자동으로 적용되는 기본값을 설정합니다.',
    syntax: '컬럼명 데이터타입 DEFAULT 기본값',
    exampleSQL: 'CREATE TABLE students (\n  id INTEGER PRIMARY KEY,\n  name VARCHAR(50) NOT NULL,\n  grade INTEGER DEFAULT 1,\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);',
    exampleResult: 'grade를 지정하지 않으면 자동으로 1이 저장됩니다. created_at은 현재 시각이 자동으로 기록됩니다.',
    relatedTerms: ['constraint', 'not-null', 'check'],
  },
]
